c_compiler_version:
  - 12
cxx_compiler_version:
  - 12
fortran_compiler_version:
  - 12

blas_impl:
  - openblas

pin_run_as_build:
  gcc:
    max_pin: x.x
  gxx:
    max_pin: x.x
  gfortran:
    max_pin: x.x
  hdf5:
    max_pin: x.x
  numpy:
    max_pin: x.x
  zlib:
    max_pin: x.x
  openmpi: x.x               # [mpi != "nompi"]

mpi:
  - openmpi                  # [unix]
  - nompi

hdf5:
  - 1.14.2=nompi*

numpy:
  - 1.26

python:
  - 3.12

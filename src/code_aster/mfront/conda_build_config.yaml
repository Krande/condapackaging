c_compiler:
  - vs2022   # [win]
  - gcc      # [unix]
c_compiler_version:
  - 8        # [unix]
cxx_compiler:
  - vs2022   # [win]
  - gxx      # [unix]
cxx_compiler_version:
  - 8        # [unix]
fortran_compiler:
  - gfortran # [unix]
fortran_compiler_version:
  - 8        # [unix]

# blas
libblas:
  - 3.9 *netlib
libcblas:
  - 3.9 *netlib
liblapack:
  - 3.9 *netlib
liblapacke:
  - 3.9 *netlib
blas_impl:
  - openblas
  - mkl          # [x86 or x86_64]
  - blis         # [x86 or x86_64]

pin_run_as_build:
  gcc:
    max_pin: x.x
  gxx:
    max_pin: x.x
  gfortran:
    max_pin: x.x
  numpy:
    max_pin: x.x
  boost:
    max_pin: x.x

numpy:
  - '1.23'
hdf5:
  - 1.10.6=nompi*
boost:
  - 1.78

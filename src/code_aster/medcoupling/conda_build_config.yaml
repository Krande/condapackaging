blas_impl:
  - openblas

pin_run_as_build:
  gcc:
    max_pin: x.x
  gxx:
    max_pin: x.x
  gfortran:
    max_pin: x.x
  numpy:
    max_pin: x.x
  libmed:
    max_pin: x.x.x
  boost:
    max_pin: x.x.x

hdf5:
  - 1.14.2=nompi*

mpi:
  - openmpi               # [not win]
  - nompi

c_compiler:                   # [win]
  - vs2022                    # [win]
cxx_compiler:                 # [win]
  - vs2022                    # [win]

c_compiler_version:       # [unix]
  - 8                     # [unix]
cxx_compiler_version:     # [unix]
  - 8                     # [unix]
fortran_compiler:         # [unix]
  - gfortran              # [unix]
fortran_compiler_version: # [unix]
  - 8                     # [unix]

blas_impl:
  - openblas

pin_run_as_build:
  gcc:
    max_pin: x.x
  gxx:
    max_pin: x.x
  gfortran:
    max_pin: x.x
  numpy:
    max_pin: x.x
  libmed:
    max_pin: x.x.x
  boost:
    max_pin: x.x.x

boost:
  - 1.78
numpy:
  - '1.25'

hdf5:
  - 1.10.6=mpi*openmpi*   # [unix]
  - 1.10.6=nompi*         # [unix]
  - 1.10.10=nompi*        # [win]

mpi:
  - openmpi               # [not win]
  - nompi

zip_keys:
  - mpi
  - hdf5

c_compiler:
  - vs2022   # [win]
  - gcc      # [unix]
c_compiler_version:
  - 10       # [unix]
cxx_compiler:
  - vs2022   # [win]
  - gxx      # [unix]
cxx_compiler_version:
  - 10       # [unix]
fortran_compiler:
  - gfortran # [unix]
fortran_compiler_version:
  - 10       # [unix]

mpi:
  - openmpi

hdf5:
  - 1.10.6=mpi*

numpy:
  - "1.25"

blas_impl:
  - openblas
  - mkl          # [x86 or x86_64]
  - blis         # [x86 or x86_64]

pin_run_as_build:
  hypre:
    max_pin: x.x.x
  metis:
    max_pin: x.x
  parmetis:
    max_pin: x.x
  scalapack:
    max_pin: x.x

c_compiler:
  - vs2022  # [win]
  - gcc     # [unix]
c_compiler_version:
  - 12      # [unix]
cxx_compiler:
  - vs2022  # [win]
  - gxx     # [unix]
cxx_compiler_version:
  - 12
fortran_compiler:
  - gfortran # [unix]
fortran_compiler_version:
  - 12       # [unix]

pin_run_as_build:
  gcc:
    max_pin: x.x
  gxx:
    max_pin: x.x
  gfortran:
    max_pin: x.x
  numpy:
    max_pin: x.x
numpy:
  - '1.23'
hdf5:
  - 1.10.6=nompi*
boost:
  - 1.78

c_compiler:
  - vs2022  # [win]
  - gcc     # [unix]
cxx_compiler:
  - vs2022  # [win]
  - gxx     # [unix]
fortran_compiler:
  - gfortran # [unix]

blas_impl:
  - openblas
  - mkl          # [x86 or x86_64]
  - blis         # [x86 or x86_64]

pin_run_as_build:
  gcc:
    max_pin: x.x
  gxx:
    max_pin: x.x
  gfortran:
    max_pin: x.x
  zlib:
    max_pin: x.x.x
  bison:
    max_pin: x.x.x
  openmpi: x.x          # [mpi != "nompi"]

mpi:
  - openmpi
  - nompi

numpy:
  - 1.25
  - 1.26

python:
  - 3.11
  - 3.12

zip_keys:
  - numpy
  - python
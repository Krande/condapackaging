name: ci-libmed

# Bump 0
on:
  push:
    paths:
      - .github/workflows/ci-libmed.yml
#    branches:
#    - donotrun

jobs:
  build:
    uses: ./.github/workflows/conda-build-basic.yml
    with:
      package_name: libmed
      boa: false
      conda_label: debug
      conda_user: krandedev
      debug_build: 'True'
    secrets: inherit
name: ci-ocp

on:
  workflow_dispatch:

jobs:
  build:
    uses: ./.github/workflows/conda-build-basic.yml
    with:
      package_name: ocp
      conda_channel: "hdf5"
      python_versions: "3.12"
      platforms: "windows-latest"
      use_quetz: "True"
    secrets: inherit
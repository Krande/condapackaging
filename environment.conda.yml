name: conda-build
channels:
  - conda-forge
  - nodefaults
dependencies:
  - conda-build
  - conda-verify
  - anaconda-client
  - pyyaml
  - ninja
  - doxygen
  - boa
  - mercurial
  # for windows and fortran compiler install the following
#    - m2w64-gcc-fortran
